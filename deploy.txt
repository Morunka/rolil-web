npm run build
cd dist
git add -A
git commit -m ''
git push -f git@github.com:Morunka/rolil-web.git master:gh-pages

OR

npm run build
git add .
git commit -m ''
npm run deploy
git push -f origin master

--

Сохранить только сайт:
npm run deploy

Сохранить только файлы:
git push -f origin master